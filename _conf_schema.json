{
    "base_url": {
        "description": "云端绘图服务地址",
        "hint": "接口站点根地址，例如 https://sd.exacg.cc",
        "type": "string",
        "default": "https://sd.exacg.cc"
    },
    "api_key": {
        "description": "云端绘图 API Key",
        "hint": "在个人中心生成的 API 密钥，按 Bearer Token 方式调用",
        "type": "string",
        "default": ""
    },
    "proxy_url": {
        "description": "代理地址",
        "hint": "可选，例如 http://127.0.0.1:7897。Node undici 桥接默认会优先使用它；Python兜底路径也会复用",
        "type": "string",
        "default": "http://127.0.0.1:7897"
    },
    "request_backend": {
        "description": "请求后端",
        "hint": "选择请求云端接口的方式。python: Python直连(已伪装UA)；node: Node undici桥接(适合服务器环境)",
        "type": "string",
        "keys": ["python", "node"],
        "default": "python"
    },
    "auto_shrink_edit_resolution": {
        "description": "编辑模型自动缩小超限原图",
        "hint": "开启后，当编辑模型且未手动指定分辨率时，如果原图单边超过 1980，则按原图宽高比自动缩小到单边不超过 1980",
        "type": "bool",
        "default": true
    },
    "auto_enlarge_edit_resolution": {
        "description": "编辑模型自动放大小图",
        "hint": "开启后，当编辑模型且未手动指定分辨率时，如果原图单边小于 1980，则按原图宽高比自动放大到尽量接近 1980；关闭后小图保持默认分辨率或手动指定分辨率",
        "type": "bool",
        "default": false
    },
    "convert_edit_result_to_png": {
        "description": "编辑结果发送前自动转 PNG",
        "hint": "开启后，编辑模型生成的结果图片在发送前会优先转为 PNG，再发送给 QQ；可提升旧版 PC QQ 的图片兼容性",
        "type": "bool",
        "default": true
    },
    "send_image_as_forward": {
        "description": "强制全部图片合并转发",
        "hint": "开启后不进行智能区分，所有生成或改图结果都以合并转发发送；优先级高于智能漏点图转发",
        "type": "bool",
        "default": false
    },
    "smart_forward_sensitive_images": {
        "description": "智能合并转发漏点图片",
        "hint": "开启后通过 Sightengine nudity-2.1 审核本地成图；仅核心裸露分达到阈值时合并转发，普通、低胸或内衣图片仍直接发送。审核重试全部失败时安全兜底为合并转发",
        "type": "bool",
        "default": false
    },
    "sightengine_credentials": {
        "description": "Sightengine 审核凭据",
        "hint": "支持多组轮询，每行格式 api_user:api_secret。审核失败重试时会自动切换下一组凭据",
        "type": "list",
        "default": [],
        "obvious_hint": true
    },
    "moderation_api_url": {
        "description": "Sightengine 审核接口地址",
        "hint": "通常无需修改，插件通过文件流上传本地成图",
        "type": "string",
        "default": "https://api.sightengine.com/1.0/check.json"
    },
    "moderation_threshold": {
        "description": "核心裸露判定阈值",
        "hint": "取 sexual_activity、sexual_display、erotica 三项最大值，达到此阈值时合并转发；不会因低胸、内衣等 suggestive 分类单独触发",
        "type": "float",
        "slider": {
            "min": 0.1,
            "max": 1.0,
            "step": 0.05
        },
        "default": 0.8
    },
    "moderation_timeout": {
        "description": "单次图片审核超时（秒）",
        "hint": "每次请求 Sightengine 的最长等待时间",
        "type": "int",
        "slider": {
            "min": 1,
            "max": 60,
            "step": 1
        },
        "default": 10
    },
    "moderation_retries": {
        "description": "图片审核失败重试次数",
        "hint": "不包含首次请求。例如设为 2 时最多请求 3 次，每次自动轮换下一组凭据；全部失败后自动合并转发",
        "type": "int",
        "slider": {
            "min": 0,
            "max": 10,
            "step": 1
        },
        "default": 2
    },
    "default_resolution": {
        "description": "默认生成图片分辨率",
        "hint": "格式: 宽x高，如 512x768, 768x512。或使用预设: portrait, landscape, square, 1024",
        "type": "string",
        "default": "512x768"
    },
    "default_steps": {
        "description": "默认生成步数",
        "hint": "普通图片模型默认 28，范围建议 1-50",
        "type": "int",
        "slider": {
            "min": 1,
            "max": 50,
            "step": 1
        },
        "default": 28
    },
    "default_cfg": {
        "description": "默认 CFG 引导强度",
        "hint": "普通图片模型建议 1.0-10.0",
        "type": "float",
        "slider": {
            "min": 1.0,
            "max": 10.0,
            "step": 0.5
        },
        "default": 7.0
    },
    "default_model_index": {
        "description": "默认文生图模型索引",
        "hint": "参考 README 中的模型表。推荐默认使用 10（wai16）。当前插件版本不支持视频模型。",
        "type": "int",
        "slider": {
            "min": 0,
            "max": 14,
            "step": 1
        },
        "default": 10
    },
    "default_edit_model_index": {
        "description": "默认编辑模型索引",
        "hint": "图片编辑时默认使用的模型索引，可按服务端实际索引自由填写，默认值为 14。",
        "type": "int",
        "default": 14
    },
    "default_negative_prompt": {
        "description": "默认负面提示词",
        "hint": "不希望出现在图片中的内容；图片编辑模型通常不会使用该字段",
        "type": "text",
        "default": "low quality, bad anatomy, bad hands, text, error, missing fingers, extra digit, fewer digits, cropped, worst quality, normal quality, jpeg artifacts, signature, watermark, username, blurry"
    },
    "timeout": {
        "description": "请求超时时间（秒）",
        "hint": "生图可能需要较长时间，建议设置 120-300 秒",
        "type": "int",
        "slider": {
            "min": 60,
            "max": 600,
            "step": 30
        },
        "default": 180
    },
    "debounce_interval": {
        "description": "防抖时长（秒）",
        "hint": "防抖时间内无法重复提交生图请求，每个用户单独计时",
        "type": "int",
        "slider": {
            "min": 0,
            "max": 120,
            "step": 5
        },
        "default": 15
    }
}